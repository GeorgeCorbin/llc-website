import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Corbin Software Solutions",
  description: "Professional web and mobile app development services. We build custom digital solutions that drive business growth.",
  keywords: "web development, mobile apps, software development, custom websites, React, Next.js",
  icons: {
    icon: "/llc_logo.png",
    shortcut: "/llc_logo.png",
    apple: "/llc_logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
